You are generating candidate commands for fuzzing.

Task:
Generate up to {{ NUMBER_OF_COMMANDS }} NEW commands that the target may accept.

Known accepted commands:
{{ EXTRACTED_ITEMS }}

Retrieved context:
{{ RAG_CONTEXT }}

Instructions:
- The known accepted commands are the primary evidence.
- Use the retrieved context only as optional supporting evidence.
- The retrieved context may be irrelevant, repetitive, incomplete, or misleading.
- Ignore any retrieved context that does not clearly help infer additional commands for the same command family.
- Do not repeat any known command.
- Do not generate a command just because it appears in the retrieved context.
- Only generate a command if it is consistent with the naming style, syntax, abbreviations, and command family suggested by the known commands.
- Prefer conservative candidates over aggressive guessing.
- If there is not enough evidence for {{ NUMBER_OF_COMMANDS }} strong candidates, return fewer.
- Do not include commands that would change the state of the system.
- Do not invent unsupported parameters.
- Make every response unique.

A retrieved chunk is useful only if it helps infer one or more of:
- a command naming pattern
- a query form such as ?
- a parameter structure
- neighboring commands from the same family
- a syntax pattern consistent with the known commands

A retrieved chunk is not useful if it:
- only repeats known commands
- contains generic product prose
- appears unrelated to the same device or command family
- is too vague to support command inference

For each generated command, include:
- command
- parameters

Return only commands that are plausible and evidence-based.